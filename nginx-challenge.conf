server {
    listen 80;
    server_name _;
    
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }
    
    location / {
        return 200 'Let\'s Encrypt challenge server - SSL certificates are being managed automatically';
        add_header Content-Type text/plain;
    }
}